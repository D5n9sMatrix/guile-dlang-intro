module arith.integer;

/*
Arithmetic
    Integer Arithmetic
    Integer arithmetic is performed using two's complement math. 
    Integer overflow is not checked for.
*/
@property void arith(integer)(ref using, performed, complete, math)
{
    void integer(
         delegate using,
         delegate performed,
         delegate complete,
         delegate math)
         {
            interface A10
            {
                delegate using;
                delegate performed;
                delegate complete;
                delegate math;
            }

            interface A11
            {
                delegate using;
                delegate performed;
                delegate complete;
                delegate math;
            }

            interface A12
            {
                delegate using;
                delegate performed;
                delegate complete;
                delegate math;
            }
         }
         
    return integer();      
}

